# Fallback when data/seo-settings.json does not exist.
# NEXT_PUBLIC_SITE_URL=https://your-verified-domain.com

# Local SEO editor only. Enable during build and start, bound to 127.0.0.1.
# Leave unset for a public deployment.
# SEO_EDITOR_ENABLED=1
